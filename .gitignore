node_modules/
.next/
out/
.env
.env.local
*.tsbuildinfo
next-env.d.ts
uploads/
data/
.vercel/
.vercel

# MCP + local Claude Code settings (may contain secrets)
.mcp.json
.claude/settings.local.json

# Separate nested SEO subsite — has its own .git and node_modules
mobile/
