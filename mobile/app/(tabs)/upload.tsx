// Upload flow — pick/record video → extract frames → enter metadata → pay → submit
// Will use expo-image-picker + expo-video-thumbnails + expo-web-browser (Stripe)
// Placeholder for full implementation
import { View, Text } from 'react-native';

export default function UploadScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#0a0a0a' }}>
      <Text style={{ color: '#fff', fontSize: 20, fontWeight: 'bold' }}>Analyze a Routine</Text>
      <Text style={{ color: '#9ca3af', marginTop: 8 }}>Upload flow — implementation pending</Text>
    </View>
  );
}
