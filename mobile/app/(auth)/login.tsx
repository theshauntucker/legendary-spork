// Login screen — placeholder for full implementation
// Will include: email + password fields, sign in button, link to signup
import { View, Text } from 'react-native';

export default function LoginScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#0a0a0a' }}>
      <Text style={{ color: '#fff', fontSize: 24, fontWeight: 'bold' }}>RoutineX</Text>
      <Text style={{ color: '#9ca3af', marginTop: 8 }}>Login screen — implementation pending</Text>
    </View>
  );
}
