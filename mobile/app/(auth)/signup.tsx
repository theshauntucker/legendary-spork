// Signup screen — placeholder for full implementation
// Will include: email + password fields, COPPA consent checkboxes, create account button
// COPPA checkboxes are REQUIRED before account creation (deadline: April 22, 2026)
import { View, Text } from 'react-native';

export default function SignupScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#0a0a0a' }}>
      <Text style={{ color: '#fff', fontSize: 24, fontWeight: 'bold' }}>Create Account</Text>
      <Text style={{ color: '#9ca3af', marginTop: 8 }}>Signup screen with COPPA consent — implementation pending</Text>
    </View>
  );
}
